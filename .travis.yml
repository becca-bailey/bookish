language: elixir
elixir: 
  - 1.3.2
install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
script:
  - mix ecto.create
  - mix ecto.migrate
  - mix run priv/repo/seeds.exs
  - mix test
