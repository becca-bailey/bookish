<%= render Bookish.SharedView, "_header.html", conn: @conn %>

<h2>New book</h2>

<%= render "form.html", changeset: @changeset,
                        locations: @locations,
                        book_metadata_titles: @book_metadata_titles,
                        action: book_path(@conn, :create) %>

